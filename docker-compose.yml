version: '3.9'

services:
  
  cytof-qc-pipeline-service:
    image: edmore/cytof-qc-pipeline
    volumes:
      - $HOME/.aws:/root/.aws:ro
    container_name: cytof-qc-pipeline
    entrypoint: aws-lambda-rie /service/main
    restart: always
    ports:
      - 9000:8080
